<Window x:Class="TextDiffViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TextDiffViewer"
        mc:Ignorable="d"
        Title="Text Diff Viewer" Height="700" Width="1200"
        Background="#F5F5F5">
    <Window.Resources>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#0078D7"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#004275"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Text Diff Viewer" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,10"/>

        <!-- Controls Panel -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Load Left File" Click="LoadLeftFile_Click" Style="{StaticResource ActionButton}"/>
            <Button Content="Load Right File" Click="LoadRightFile_Click" Style="{StaticResource ActionButton}"/>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0">
                <CheckBox Content="Show Line Numbers" IsChecked="{Binding ShowLineNumbers}" VerticalAlignment="Center" Margin="10,0"/>
                <TextBlock Text="Context Lines:" VerticalAlignment="Center" Margin="10,0,5,0"/>
                <ComboBox SelectedValue="{Binding ContextLines}" Width="50">
                    <ComboBoxItem Content="0"/>
                    <ComboBoxItem Content="1"/>
                    <ComboBoxItem Content="3"/>
                    <ComboBoxItem Content="5"/>
                    <ComboBoxItem Content="10"/>
                </ComboBox>
            </StackPanel>
        </StackPanel>

        <!-- Diff Panels -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Side -->
            <Grid Grid.Column="0" Margin="0,0,5,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Original Text" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,5"/>

                <TextBox Grid.Row="1" Text="{Binding LeftText, UpdateSourceTrigger=PropertyChanged}" 
                         AcceptsReturn="True" TextWrapping="Wrap" MinHeight="100" MaxHeight="200"
                         Padding="5" BorderThickness="1" BorderBrush="#CCCCCC"/>

                <Border Grid.Row="2" BorderThickness="1" BorderBrush="#CCCCCC" Background="White" Margin="0,10,0,0">
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="leftDiffPanel" Margin="5"/>
                    </ScrollViewer>
                </Border>
            </Grid>

            <!-- Right Side -->
            <Grid Grid.Column="1" Margin="5,0,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Modified Text" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,5"/>

                <TextBox Grid.Row="1" Text="{Binding RightText, UpdateSourceTrigger=PropertyChanged}" 
                         AcceptsReturn="True" TextWrapping="Wrap" MinHeight="100" MaxHeight="200"
                         Padding="5" BorderThickness="1" BorderBrush="#CCCCCC"/>

                <Border Grid.Row="2" BorderThickness="1" BorderBrush="#CCCCCC" Background="White" Margin="0,10,0,0">
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="rightDiffPanel" Margin="5"/>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Grid>

        <!-- Legend -->
        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#FFC7CE" Width="20" Height="20" Margin="5,0"/>
            <TextBlock Grid.Column="1" Text="Deleted" VerticalAlignment="Center" Margin="5,0,15,0"/>

            <Border Grid.Column="2" Background="#C6EFCE" Width="20" Height="20" Margin="5,0"/>
            <TextBlock Grid.Column="3" Text="Added" VerticalAlignment="Center" Margin="5,0"/>
        </Grid>
    </Grid>
</Window>